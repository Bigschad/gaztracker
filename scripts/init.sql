-- =============================================================================
-- PostgreSQL Initialization Script for GazTracker
-- =============================================================================
-- This file is executed automatically when the PostgreSQL container is first created.
-- It runs before Alembic migrations.
--
-- You can add custom SQL commands here if needed (e.g., creating extensions,
-- setting up initial data, etc.)
--
-- Note: The database schema is managed by Alembic migrations, so most
-- initialization should be done there.
-- =============================================================================

-- Example: Enable PostgreSQL extensions if needed
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
-- CREATE EXTENSION IF NOT EXISTS "pg_trgm";

